@php
    //dddx([$form_data, $fields]);

@endphp
<div>
    @foreach($fields as $field)
        <td>
            {{ $field->setPrefix('rows.'.$index)->html($form_data, $row) }}
        </td>
    @endforeach
</div>